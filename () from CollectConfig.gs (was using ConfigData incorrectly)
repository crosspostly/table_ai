[1mdiff --git a/deploy/CollectConfig.gs b/deploy/CollectConfig.gs[m
[1mindex 77caf743..63023231 100644[m
[1m--- a/deploy/CollectConfig.gs[m
[1m+++ b/deploy/CollectConfig.gs[m
[36m@@ -15,7 +15,7 @@[m [mconst COLLECT_CONFIG_LAST_UPDATE = '2025-11-20 12:22:00';[m
 // ============================================================================[m
 // ГЛОБАЛЬНЫЙ ЛОГ (для передачи в UI)[m
 // ============================================================================[m
[31m-var GLOBAL_LOG = [];[m
[32m+[m[32mlet GLOBAL_LOG = [];[m
 [m
 function addLog(message, level) {[m
   level = level || 'INFO';[m
[36m@@ -69,8 +69,8 @@[m [mfunction getCollectConfigInitData() {[m
 [m
     const sheetName = sheet.getName();[m
     const cellAddress = range.getA1Notation();[m
[31m-    const sheets = SpreadsheetApp.getActiveSpreadsheet().getSheets().map(function(s) { [m
[31m-      return s.getName(); [m
[32m+[m[32m    const sheets = SpreadsheetApp.getActiveSpreadsheet().getSheets().map(function(s) {[m
[32m+[m[32m      return s.getName();[m
     });[m
 [m
     addLog(`📍 Целевая ячейка: ${sheetName}!${cellAddress}`, 'INFO');[m
[36m@@ -200,7 +200,7 @@[m [mfunction executeCollectConfig(sheetName, cellAddress) {[m
     addLog('📖 Конфигурация загружена', 'INFO');[m
 [m
     // Собираем System Prompt[m
[31m-    var systemPrompt = '';[m
[32m+[m[32m    let systemPrompt = '';[m
     if (config.systemPrompt && config.systemPrompt.sheet && config.systemPrompt.cell) {[m
       addLog(`📍 System Prompt: ${config.systemPrompt.sheet}!${config.systemPrompt.cell}`, 'INFO');[m
       try {[m
[36m@@ -237,7 +237,7 @@[m [mfunction executeCollectConfig(sheetName, cellAddress) {[m
     }[m
 [m
     // Формируем финальный промпт[m
[31m-    var finalPrompt = '';[m
[32m+[m[32m    let finalPrompt = '';[m
     if (systemPrompt) {[m
       finalPrompt += systemPrompt + '\n\n---\n\n';[m
     }[m
[36m@@ -306,8 +306,8 @@[m [mfunction readData(sheetName, cellAddress) {[m
 [m
     // Превращаем в плоский массив и фильтруем пустые[m
     const result = [];[m
[31m-    for (var r = 0; r < values.length; r++) {[m
[31m-      for (var c = 0; c < values[r].length; c++) {[m
[32m+[m[32m    for (let r = 0; r < values.length; r++) {[m
[32m+[m[32m      for (let c = 0; c < values[r].length; c++) {[m
         const val = values[r][c];[m
         if (val !== null && val !== undefined && val.toString().trim() !== '') {[m
           result.push(val.toString());[m
[36m@@ -330,7 +330,7 @@[m [mfunction readData(sheetName, cellAddress) {[m
 function saveCollectConfig(sheetName, cellAddress, config) {[m
   try {[m
     const ss = SpreadsheetApp.getActiveSpreadsheet();[m
[31m-    var configSheet = ss.getSheetByName('ConfigData');[m
[32m+[m[32m    let configSheet = ss.getSheetByName('ConfigData');[m
 [m
     if (!configSheet) {[m
       configSheet = ss.insertSheet('ConfigData');[m
[36m@@ -346,8 +346,8 @@[m [mfunction saveCollectConfig(sheetName, cellAddress, config) {[m
 [m
     // Ищем существующую строку[m
     const data = configSheet.getDataRange().getValues();[m
[31m-    var rowIndex = -1;[m
[31m-    for (var i = 1; i < data.length; i++) {[m
[32m+[m[32m    let rowIndex = -1;[m
[32m+[m[32m    for (let i = 1; i < data.length; i++) {[m
       if (data[i][0] === sheetName && data[i][1] === cellAddress) {[m
         rowIndex = i + 1;[m
         break;[m
[36m@@ -389,9 +389,9 @@[m [mfunction loadCollectConfig(sheetName, cellAddress) {[m
     }[m
 [m
     const data = configSheet.getDataRange().getValues();[m
[31m-    for (var i = 1; i < data.length; i++) {[m
[32m+[m[32m    for (let i = 1; i < data.length; i++) {[m
       if (data[i][0] === sheetName && data[i][1] === cellAddress) {[m
[31m-        var userData = [];[m
[32m+[m[32m        let userData = [];[m
         try {[m
           if (data[i][4]) {[m
             userData = JSON.parse(data[i][4]);[m
[36m@@ -427,7 +427,7 @@[m [mfunction updateLastRun(sheetName, cellAddress) {[m
     }[m
 [m
     const data = configSheet.getDataRange().getValues();[m
[31m-    for (var i = 1; i < data.length; i++) {[m
[32m+[m[32m    for (let i = 1; i < data.length; i++) {[m
       if (data[i][0] === sheetName && data[i][1] === cellAddress) {[m
         configSheet.getRange(i + 1, 7).setValue(new Date().toISOString());[m
         return;[m
[36m@@ -485,7 +485,7 @@[m [mfunction refreshCellWithConfig() {[m
       const response = ui.alert([m
         '⚠️ Конфигурация не найдена',[m
         'Хотите создать новую?',[m
[31m-        ui.ButtonSet.YES_NO[m
[32m+[m[32m        ui.ButtonSet.YES_NO,[m
       );[m
 [m
       if (response === ui.Button.YES) {[m
[36m@@ -517,7 +517,7 @@[m [mfunction serverGetAllTemplates() {[m
     const templates = getAllTemplates(user);[m
 [m
     const result = {};[m
[31m-    for (var name in templates) {[m
[32m+[m[32m    for (const name in templates) {[m
       result[name] = templates[name].config || templates[name];[m
     }[m
 [m
[36m@@ -558,8 +558,8 @@[m [mfunction serverDeleteTemplate(templateName) {[m
 // ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ[m
 // ============================================================================[m
 function getAllSheetNames() {[m
[31m-  return SpreadsheetApp.getActiveSpreadsheet().getSheets().map(function(s) { [m
[31m-    return s.getName(); [m
[32m+[m[32m  return SpreadsheetApp.getActiveSpreadsheet().getSheets().map(function(s) {[m
[32m+[m[32m    return s.getName();[m
   });[m
 }[m
 [m
[36m@@ -620,8 +620,8 @@[m [mfunction updateReflectionConfigs() {[m
 [m
     addLog(`📋 Найдено конфигураций: ${reflectionConfigs.length}`, 'INFO');[m
 [m
[31m-    var successCount = 0;[m
[31m-    var errorCount = 0;[m
[32m+[m[32m    let successCount = 0;[m
[32m+[m[32m    let errorCount = 0;[m
     const errors = [];[m
 [m
     // Выполняем каждую конфигурацию[m
[36m@@ -652,7 +652,7 @@[m [mfunction updateReflectionConfigs() {[m
     addLog(`📊 ИТОГО: ✅ ${successCount} успешно, ❌ ${errorCount} с ошибками`, 'INFO');[m
 [m
     // Показываем результат[m
[31m-    var message = `Обновление рефлексии завершено:\n\n✅ Успешно: ${successCount}\n❌ С ошибками: ${errorCount}`;[m
[32m+[m[32m    let message = `Обновление рефлексии завершено:\n\n✅ Успешно: ${successCount}\n❌ С ошибками: ${errorCount}`;[m
 [m
     if (errors.length > 0 && errors.length <= 5) {[m
       message += '\n\nОшибки:\n' + errors.slice(0, 5).join('\n');[m
[36m@@ -664,7 +664,7 @@[m [mfunction updateReflectionConfigs() {[m
     SpreadsheetApp.getUi().alert([m
       errorCount > 0 ? '⚠️ Обновление завершено с ошибками' : '✅ Обновление завершено успешно',[m
       message,[m
[31m-      SpreadsheetApp.getUi().ButtonSet.OK[m
[32m+[m[32m      SpreadsheetApp.getUi().ButtonSet.OK,[m
     );[m
   } catch (error) {[m
     addLog(`💥 Критическая ошибка: ${error.message}`, 'ERROR');[m
[36m@@ -672,94 +672,5 @@[m [mfunction updateReflectionConfigs() {[m
   }[m
 }[m
 [m
[31m-/**[m
[31m- * Обновить распаковку - выполняет все конфигурации для листа "Распаковка"[m
[31m- */[m
[31m-function updateUnpackingConfigs() {[m
[31m-  try {[m
[31m-    clearLog();[m
[31m-    addLog('🚀 Обновление распаковки', 'INFO');[m
[31m-    addLog('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━', 'INFO');[m
[31m-[m
[31m-    const ss = SpreadsheetApp.getActiveSpreadsheet();[m
[31m-    const configSheet = ss.getSheetByName('ConfigData');[m
[31m-[m
[31m-    if (!configSheet) {[m
[31m-      SpreadsheetApp.getUi().alert('❌ Ошибка', 'Лист "ConfigData" не найден!', SpreadsheetApp.getUi().ButtonSet.OK);[m
[31m-      return;[m
[31m-    }[m
[31m-[m
[31m-    const data = configSheet.getDataRange().getValues();[m
[31m-    const unpackingConfigs = [];[m
[31m-[m
[31m-    // Собираем все конфигурации для листа "Распаковка"[m
[31m-    for (var i = 1; i < data.length; i++) {[m
[31m-      if (data[i][0] === 'Распаковка') { // Sheet column[m
[31m-        unpackingConfigs.push({[m
[31m-          sheetName: data[i][0],[m
[31m-          cellAddress: data[i][1],[m
[31m-          systemPromptSheet: data[i][2],[m
[31m-          systemPromptCell: data[i][3],[m
[31m-          userDataJSON: data[i][4],[m
[31m-        });[m
[31m-      }[m
[31m-    }[m
[31m-[m
[31m-    if (unpackingConfigs.length === 0) {[m
[31m-      SpreadsheetApp.getUi().alert('ℹ️ Информация', 'Конфигурации для распаковки не найдены', SpreadsheetApp.getUi().ButtonSet.OK);[m
[31m-      return;[m
[31m-    }[m
[31m-[m
[31m-    addLog(`📋 Найдено конфигураций: ${unpackingConfigs.length}`, 'INFO');[m
[31m-[m
[31m-    var successCount = 0;[m
[31m-    var errorCount = 0;[m
[31m-    const errors = [];[m
[31m-[m
[31m-    // Выполняем каждую конфигурацию[m
[31m-    for (var i = 0; i < unpackingConfigs.length; i++) {[m
[31m-      const config = unpackingConfigs[i];[m
[31m-      addLog(`\n🔄 Обработка ${i + 1}/${unpackingConfigs.length}: ${config.sheetName}!${config.cellAddress}`, 'INFO');[m
[31m-[m
[31m-      try {[m
[31m-        const result = executeCollectConfig(config.sheetName, config.cellAddress);[m
[31m-        if (result.success) {[m
[31m-          successCount++;[m
[31m-          addLog(`✅ Успешно: ${config.sheetName}!${config.cellAddress}`, 'SUCCESS');[m
[31m-        } else {[m
[31m-          errorCount++;[m
[31m-          const errorMsg = `❌ Ошибка в ${config.sheetName}!${config.cellAddress}: ${result.error}`;[m
[31m-          addLog(errorMsg, 'ERROR');[m
[31m-          errors.push(errorMsg);[m
[31m-        }[m
[31m-      } catch (e) {[m
[31m-        errorCount++;[m
[31m-        const errorMsg = `💥 Исключение в ${config.sheetName}!${config.cellAddress}: ${e.message}`;[m
[31m-        addLog(errorMsg, 'ERROR');[m
[31m-        errors.push(errorMsg);[m
[31m-      }[m
[31m-    }[m
[31m-[m
[31m-    addLog('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━', 'INFO');[m
[31m-    addLog(`📊 ИТОГО: ✅ ${successCount} успешно, ❌ ${errorCount} с ошибками`, 'INFO');[m
[31m-[m
[31m-    // Показываем результат[m
[31m-    var message = `Обновление распаковки завершено:\n\n✅ Успешно: ${successCount}\n❌ С ошибками: ${errorCount}`;[m
[31m-[m
[31m-    if (errors.length > 0 && errors.length <= 5) {[m
[31m-      message += '\n\nОшибки:\n' + errors.slice(0, 5).join('\n');[m
[31m-    } else if (errors.length > 5) {[m
[31m-      message += '\n\nПервые 5 ошибок:\n' + errors.slice(0, 5).join('\n');[m
[31m-      message += `\n... и еще ${errors.length - 5} ошибок`;[m
[31m-    }[m
[31m-[m
[31m-    SpreadsheetApp.getUi().alert([m
[31m-      errorCount > 0 ? '⚠️ Обновление завершено с ошибками' : '✅ Обновление завершено успешно',[m
[31m-      message,[m
[31m-      SpreadsheetApp.getUi().ButtonSet.OK[m
[31m-    );[m
[31m-  } catch (error) {[m
[31m-    addLog(`💥 Критическая ошибка: ${error.message}`, 'ERROR');[m
[31m-    SpreadsheetApp.getUi().alert('❌ Критическая ошибка', error.message, SpreadsheetApp.getUi().ButtonSet.OK);[m
[31m-  }[m
[31m-}[m
[32m+[m[32m// Функция updateUnpackingConfigs() перенесена в UnpackingViewer.gs[m
[32m+[m[32m// Она должна работать напрямую с листом "Распаковка", а не через систему ConfigData[m
